# Phase 4.5: Supply-chain hardening (pnpm migration) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Migrate v1-archive root from npm → pnpm with `minimumReleaseAge: 4320` + `onlyBuiltDependencies: [better-sqlite3]`. Update Dockerfile root globals to pnpm (agent-runner stays npm). Update CI to pnpm. Add Supply Chain Security section to CLAUDE.md.

**Architecture:** Two-package layout — root pnpm (host), `container/agent-runner/` npm (in-container). `.npmrc` `minimumReleaseAge=4320` already exists from Phase 1 and acts as fallback for npm-direct invocations.

**Tech Stack:** Node 22, pnpm 9.15.0 (root), npm (container/agent-runner). Lockfile flip: `package-lock.json` → `pnpm-lock.yaml`.

**Spec input:** `/data/nanotars/docs/superpowers/specs/2026-04-26-phase-4.5-supply-chain.md`

---

## Pre-flight verification

- [ ] **Step 1: Clean tree on `v1-archive`, Phase 4 complete**

```
cd /data/nanotars && git status --short --branch && git log --oneline -5
```

Expected: clean tree, HEAD shows Phase 4 commits.

- [ ] **Step 2: Capture baseline test counts + lockfile state**

```
cd /data/nanotars && npm test 2>&1 | tail -5
cd /data/nanotars/container/agent-runner && npm test 2>&1 | tail -5
cd /data/nanotars && ls -la package-lock.json pnpm-lock.yaml pnpm-workspace.yaml 2>&1
cd /data/nanotars && cat .npmrc
```

Expected: 839 host tests, 48 container tests. `package-lock.json` exists. `pnpm-lock.yaml` + `pnpm-workspace.yaml` do NOT exist. `.npmrc` already has `minimumReleaseAge=4320` + `engine-strict=true`.

- [ ] **Step 3: pnpm 9.x available**

```
which pnpm && pnpm --version
```

If missing or older than 9.x: `npm install -g pnpm@9.15.0`.

- [ ] **Step 4: Clear stale node_modules if previously contaminated**

```
cd /data/nanotars && rm -rf node_modules
```

---

## Task 1: Add `pnpm-workspace.yaml`

**Files:** `/data/nanotars/pnpm-workspace.yaml` (new)

- [ ] **Step 1: Create file with this exact content:**

```yaml
# Root package only. container/agent-runner/ is intentionally NOT a workspace
# member — it has its own package-lock.json and is installed inside the agent
# container by the Dockerfile's npm install step.
onlyBuiltDependencies:
  - better-sqlite3

pnpm:
  minimumReleaseAge: 4320
```

- [ ] **Step 2: Commit**

```
cd /data/nanotars && git add pnpm-workspace.yaml && git commit -m "chore(pnpm): add pnpm-workspace.yaml with minReleaseAge=4320 + onlyBuiltDependencies"
```

---

## Task 2: Convert lockfile via `pnpm import`

**Files:** `/data/nanotars/pnpm-lock.yaml` (new), `/data/nanotars/package-lock.json` (delete)

CRITICAL: Use `pnpm import` (preserves resolved versions). Do NOT run a fresh `pnpm install` first — that re-resolves and risks the native-binding clobber that broke a Phase 3 attempt (107 test failures).

- [ ] **Step 1: Run `pnpm import`**

```
cd /data/nanotars && pnpm import
```

Expected: `Importing package-lock.json` line; `pnpm-lock.yaml` appears.

- [ ] **Step 2: Install from new lockfile**

```
cd /data/nanotars && pnpm install --frozen-lockfile
```

Expected: completes with no missing-binding errors. better-sqlite3 rebuilds.

- [ ] **Step 3: Verify better-sqlite3 binding**

```
cd /data/nanotars && node -e "const db=require('better-sqlite3')(':memory:'); console.log(db.prepare('SELECT 1 AS x').get());"
```

Expected: `{ x: 1 }`. If it throws `MODULE_NOT_FOUND` or version mismatch — STOP, the clobber is happening.

- [ ] **Step 4: Tests match baseline**

```
cd /data/nanotars && pnpm test 2>&1 | tail -5
```

Expected: 839 passing. Any drift → STOP and diagnose.

- [ ] **Step 5: Delete old lockfile + commit**

```
cd /data/nanotars && rm package-lock.json
cd /data/nanotars && git add pnpm-lock.yaml && git rm package-lock.json && git commit -m "chore(pnpm): convert package-lock.json to pnpm-lock.yaml via pnpm import"
```

---

## Task 3: Verify root `package.json` scripts under pnpm

**Files:** `/data/nanotars/package.json` (only edit if Step 1 fails)

- [ ] **Step 1: Run full chain**

```
cd /data/nanotars && pnpm run test:all 2>&1 | tail -10
```

Expected: typecheck + 839 tests + bash tests all pass. If green, skip to next task (no commit).

- [ ] **Step 2: ONLY IF Step 1 fails — switch `test:all` to pnpm**

In `/data/nanotars/package.json`, replace:

```json
"test:all": "npm run typecheck && npm run test && npm run test:bash"
```

with:

```json
"test:all": "pnpm run typecheck && pnpm test && pnpm run test:bash"
```

Re-run, then:

```
cd /data/nanotars && git add package.json && git commit -m "chore(pnpm): switch test:all to pnpm run for manager consistency"
```

---

## Task 4: Update `container/Dockerfile` — pnpm bootstrap + global installs

**Files:** `/data/nanotars/container/Dockerfile`

The Dockerfile does NOT install root host deps — only agent-runner (line ~62, stays npm) + globals (claude-code, agent-browser, tsx — migrate to pnpm).

- [ ] **Step 1: Add `PNPM_VERSION` ARG**

Find:
```dockerfile
ARG TSX_VERSION=4.17.0
```
Replace with:
```dockerfile
ARG TSX_VERSION=4.17.0
ARG PNPM_VERSION=9.15.0
```

- [ ] **Step 2: Bootstrap pnpm + migrate agent-browser/claude-code globals**

Find:
```dockerfile
# Install agent-browser and claude-code globally
RUN npm install -g agent-browser@${AGENT_BROWSER_VERSION} @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}
```

Replace with:
```dockerfile
# Bootstrap pnpm via npm (the .npmrc minReleaseAge=3d gates this npm-side
# install), then use pnpm for the rest so the supply-chain policy applies.
RUN npm install -g pnpm@${PNPM_VERSION}

# Install agent-browser and claude-code globally via pnpm
RUN pnpm install -g agent-browser@${AGENT_BROWSER_VERSION} @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}
```

- [ ] **Step 3: Migrate tsx global install**

Find:
```dockerfile
RUN npm install -g tsx@${TSX_VERSION}
```
Replace with:
```dockerfile
RUN pnpm install -g tsx@${TSX_VERSION}
```

- [ ] **Step 4: Add intent comment to agent-runner npm install (stays npm)**

Find:
```dockerfile
# Install dependencies (devDeps pruned below; kept here for npm install compatibility)
RUN npm install
```
Replace with:
```dockerfile
# agent-runner is a separate package — installed with npm (not pnpm). Its own
# package-lock.json + the .npmrc minReleaseAge=3d at repo root provide the
# 3-day supply-chain hold. Do not migrate this to pnpm without making
# agent-runner a workspace member.
RUN npm install
```

- [ ] **Step 5: Sanity-build the container**

```
cd /data/nanotars && ./container/build.sh 2>&1 | tail -20
```

Expected: build succeeds, final line `Image: nanoclaw-agent:latest`. If `pnpm install -g <pkg>` errors, fall back to `pnpm add -g <pkg>` (alias in pnpm 9).

- [ ] **Step 6: Commit**

```
cd /data/nanotars && git add container/Dockerfile && git commit -m "chore(docker): migrate root global installs to pnpm; pin pnpm@9.15.0"
```

---

## Task 5: Update `.github/workflows/ci.yml` to pnpm

**Files:** `/data/nanotars/.github/workflows/ci.yml`

`host` + `bash-tests` jobs → pnpm. `agent-runner` job stays npm.

- [ ] **Step 1: Replace entire file content with:**

```yaml
name: CI

on:
  push:
    branches: [main, v1-archive]
  pull_request:
    branches: [main, v1-archive]

jobs:
  host:
    name: Host (Node 22, pnpm)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run typecheck
      - run: pnpm test

  agent-runner:
    name: Container agent-runner (Node 22, npm)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - working-directory: container/agent-runner
        run: npm install --no-fund
      - working-directory: container/agent-runner
        run: npx tsc --noEmit
      - working-directory: container/agent-runner
        run: npm test

  bash-tests:
    name: Bash tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run test:bash
```

- [ ] **Step 2: Validate YAML + commit**

```
cd /data/nanotars && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "yaml valid"
cd /data/nanotars && git add .github/workflows/ci.yml && git commit -m "ci: migrate host + bash-tests jobs to pnpm with action-setup@v4"
```

---

## Task 6: Add "Supply Chain Security" section to `CLAUDE.md`

**Files:** `/data/nanotars/CLAUDE.md`

- [ ] **Step 1: Insert section before `## Container Build Cache`**

Use Edit. Find:
```
## Container Build Cache
```
Replace with:
```
## Supply Chain Security (pnpm)

This project uses pnpm at the repo root with `minimumReleaseAge: 4320` (3 days) in `pnpm-workspace.yaml`. New package versions must exist on the npm registry for 3 days before pnpm will resolve them. The `container/agent-runner/` subpackage stays on npm and gets the same 3-day hold via `.npmrc` (`minimumReleaseAge=4320`).

**Two-package layout:**
- **Root (`/data/nanotars/`)** — pnpm. Lockfile: `pnpm-lock.yaml`. Install: `pnpm install --frozen-lockfile`.
- **Container agent-runner (`container/agent-runner/`)** — npm. Lockfile: `package-lock.json`. Install: `npm install` (run inside the Docker build).

**Rules — do not bypass without explicit human approval:**
- **`minimumReleaseAgeExclude`**: Never add entries without human sign-off. If a package must bypass the release-age gate, the human must approve and pin the exact version (e.g. `package@1.2.3`), never a range.
- **`onlyBuiltDependencies`**: Never add packages without human approval — build scripts execute arbitrary code during install. Current allowlist: `[better-sqlite3]`.
- **`pnpm install --frozen-lockfile`** in CI, automation, and container builds. Never bare `pnpm install` in those contexts.
- **Lockfile conversion**: Use `pnpm import` (not `pnpm install`) to generate `pnpm-lock.yaml` from a legacy `package-lock.json`. Fresh `pnpm install` re-resolves versions and risks native-binding clobbers.

## Container Build Cache
```

- [ ] **Step 2: Commit**

```
cd /data/nanotars && git add CLAUDE.md && git commit -m "docs: add Supply Chain Security section to CLAUDE.md"
```

---

## Task 7: End-to-end sanity test

**Files:** none — validation gate.

- [ ] **Step 1: Fresh install + typecheck + test + build**

```
cd /data/nanotars && rm -rf node_modules && pnpm install --frozen-lockfile 2>&1 | tail -5
cd /data/nanotars && pnpm run typecheck
cd /data/nanotars && pnpm test 2>&1 | tail -5
cd /data/nanotars && pnpm run build 2>&1 | tail -5
```

Expected: install clean, typecheck silent (exit 0), 839 host tests pass, build emits `dist/`.

- [ ] **Step 2: Container build + agent-runner tests**

```
cd /data/nanotars && ./container/build.sh 2>&1 | tail -10
cd /data/nanotars/container/agent-runner && npm test 2>&1 | tail -5
```

Expected: image builds, 48 container tests pass.

- [ ] **Step 3: Git status clean**

```
cd /data/nanotars && git status --short
```

Expected: empty (or only ignored `node_modules/`, `dist/`).

- [ ] **Step 4: No commit unless a fix-up was needed**

If a fix-up was needed in any sub-step, commit it as `chore(pnpm): <fix description>`.

---

## Task 8: Update memory file

**Files:** `/root/.claude/projects/-data-nanoclaw-v2/memory/project-v1-package-manager.md`

- [ ] **Step 1: Replace memory file content with:**

```
# v1-archive package manager (post-Phase-4.5)

- **Root** uses **pnpm** (`pnpm-lock.yaml`, `pnpm-workspace.yaml`).
  - `pnpm install --frozen-lockfile` is the install command. Never bare `pnpm install` in CI / Dockerfile / automation.
  - `minimumReleaseAge: 4320` (3 days) enforced via `pnpm-workspace.yaml`.
  - `onlyBuiltDependencies: [better-sqlite3]` — narrowest viable allowlist.
- **container/agent-runner/** stays on **npm** (`package-lock.json`).
  - Installed inside the Docker image by the existing `RUN npm install` step.
  - 3-day hold comes from repo root `.npmrc` (`minimumReleaseAge=4320`).
- **Lockfile conversion lesson:** Use `pnpm import` (preserves resolved versions from `package-lock.json`), never a fresh `pnpm install` (re-resolves and risks native-binding clobber — caused 107 test failures in a Phase 3 attempt).
- **Dockerfile:** root global installs (`agent-browser`, `claude-code`, `tsx`) use `pnpm install -g` after a `npm install -g pnpm@X` bootstrap. Agent-runner's `npm install` line stays untouched.
- **CI:** `host` + `bash-tests` jobs use `pnpm/action-setup@v4` + `setup-node@v4` (cache: pnpm). `agent-runner` job stays on npm.
```

- [ ] **Step 2: No commit** — memory files are not part of the v1-archive repo.

---

## Phase 4.5 final review

- [ ] `pnpm-workspace.yaml` exists with `minimumReleaseAge: 4320` + `onlyBuiltDependencies: [better-sqlite3]`.
- [ ] `pnpm-lock.yaml` exists; `package-lock.json` is gone.
- [ ] `.npmrc` unchanged.
- [ ] `Dockerfile` bootstraps pnpm via npm; globals via pnpm; agent-runner `npm install` untouched (with comment).
- [ ] `ci.yml` uses `pnpm/action-setup@v4` (pinned 9.15.0) for host + bash-tests jobs.
- [ ] `CLAUDE.md` has "Supply Chain Security (pnpm)" section.
- [ ] `pnpm test` = 839, `npm test` (agent-runner) = 48, no drift.
- [ ] `./container/build.sh` succeeds.
- [ ] Phase commit count: ~5–7 (Tasks 1, 2, 4, 5, 6 always; Task 3, 7 conditional).

If any item fails, fix in a follow-up commit. Never amend.

**Cross-tier review:** Dockerfile change in Task 4 is verified by the container build in Task 7. No separate reviewer dispatch needed unless Task 7 reveals issues.
